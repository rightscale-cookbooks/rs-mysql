LoadPlugin exec
<Plugin exec>
  # userid plugin executable plugin args
  #   Exec "my_user" "/usr/lib/collectd/plugins/mycpuload.rb"
  <% node['rs-mysql']['exec'].each do |exec| %>
  <%= exec %>
  <% end %>
</Plugin>
